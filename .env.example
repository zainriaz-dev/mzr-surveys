# =============================================================================
# Environment Configuration Template
# =============================================================================
# This file contains all environment variables used by the application.
# Copy this file to .env.local and fill in your actual values.
# NEVER commit real secrets to git!

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Base URL for the application (used for share URLs, redirects, etc.)
# In development: http://localhost:3000
# In production: https://yourdomain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Node environment (automatically set by Next.js)
NODE_ENV=development

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# Admin password for accessing admin panel (/admin)
# Choose a strong password! This protects your admin dashboard.
ADMIN_PASSWORD=YOUR_SECURE_ADMIN_PASSWORD

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# MongoDB connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# OR for local: mongodb://localhost:27017/survey
MONGODB_URL=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority&appName=YOUR_APP

# Alternative MongoDB URI (if MONGODB_URL is not set, this will be used)
MONGODB_URI=mongodb://localhost:27017/survey

# MongoDB database name (defaults to "survey" if not specified)
MONGODB_DB=survey

# =============================================================================
# AI SERVICES CONFIGURATION
# =============================================================================

# Provider Order - Comma-separated list defining fallback priority
# Options: azure_openai_primary,azure_openai_secondary,gemini,deepseek
AI_PROVIDER_ORDER=azure_openai_primary,azure_openai_secondary,gemini,deepseek

# AI Request Parameters
AI_TEMPERATURE=0.2          # Randomness in responses (0.0-1.0)
AI_TOP_P=0.9               # Nucleus sampling parameter (0.0-1.0)
AI_MAX_TOKENS=800          # Maximum tokens in AI responses

# Legacy provider configuration (for backward compatibility)
PRIMARY_AI_PROVIDER=azure_openai
FALLBACK_AI_PROVIDER=gemini

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (Primary)
# -----------------------------------------------------------------------------

# Primary Azure OpenAI endpoint
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/

# Primary Azure OpenAI API key
AZURE_OPENAI_API_KEY=YOUR_AZURE_OPENAI_API_KEY

# Primary Azure OpenAI deployment name
AZURE_OPENAI_DEPLOYMENT_NAME=model-router

# Azure OpenAI API version
AZURE_OPENAI_API_VERSION=2024-06-01

# Primary Azure OpenAI model
AZURE_OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (Secondary/Backup)
# -----------------------------------------------------------------------------

# Secondary Azure OpenAI endpoint (for fallback)
AZURE_OPENAI_ENDPOINT_2=https://your-backup-resource.openai.azure.com/

# Secondary Azure OpenAI API key
AZURE_OPENAI_API_KEY_2=YOUR_AZURE_OPENAI_API_KEY_2

# Secondary Azure OpenAI deployment name
AZURE_OPENAI_DEPLOYMENT_NAME_2=model-router

# Secondary Azure OpenAI model
AZURE_OPENAI_MODEL_2=gpt-4o

# -----------------------------------------------------------------------------
# Google Gemini Configuration
# -----------------------------------------------------------------------------

# Google Gemini API key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Gemini model to use
GEMINI_MODEL=gemini-1.5-flash-latest

# -----------------------------------------------------------------------------
# DeepSeek Configuration
# -----------------------------------------------------------------------------

# DeepSeek API key
# Get from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=YOUR_DEEPSEEK_API_KEY

# DeepSeek model to use
DEEPSEEK_MODEL=deepseek-chat

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================

# Enable debug logging (optional)
DEBUG=false

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. AI Provider Priority:
#    The application will try providers in the order specified by AI_PROVIDER_ORDER.
#    If a provider fails or is rate-limited, it moves to the next one.
#
# 2. Required vs Optional:
#    - ADMIN_PASSWORD: Required for admin access
#    - MONGODB_URL/MONGODB_URI: Required for database functionality
#    - At least one AI provider API key is required for AI features
#
# 3. Security:
#    - Keep this file (.env.example) in git as a template
#    - Never commit .env.local (it should be in .gitignore)
#    - Use strong passwords and keep API keys secret
#
# 4. Local Development:
#    - Copy this file: cp .env.example .env.local
#    - Fill in your actual values in .env.local
#    - The application will automatically load .env.local
